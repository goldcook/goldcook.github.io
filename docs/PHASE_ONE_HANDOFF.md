# Goldcook 个人主页一期交接文档

更新日期：2026-09-12

正式网站：<https://goldcook.github.io/>

公开仓库：<https://github.com/goldcook/goldcook.github.io>

私有完整备份：`goldcook/goldcook-homepage-private`（需要仓库权限）

这份文档主要写给下一位接手开发的 Agent。它不依赖本次对话、当前电脑或当前工作目录；只要能够访问 GitHub 仓库，就应该可以据此恢复项目、理解边界、完成修改并安全发布。

## 0. 新环境接手指南

### 0.1 先选择正确的仓库

公开仓库是线上网站的发布源：

```bash
git clone https://github.com/goldcook/goldcook.github.io.git
cd goldcook.github.io
```

私有仓库是完整备份，包含公开站点和 `private/` 私人档案。需要处理私人时间线时，应另外克隆到独立目录：

```bash
git clone https://github.com/goldcook/goldcook-homepage-private.git
cd goldcook-homepage-private
```

两个仓库有不同的 Git 历史，不能互相合并，也不能把私有仓库设置成公开网站的远程仓库。不要在同一个工作目录里来回切换两个远程。

### 0.2 配置提交身份

这个项目统一使用：

```bash
git config user.name "goldcook"
git config user.email "goldcook4@gmail.com"
```

不要使用其他工作邮箱、机器默认邮箱或旧提交身份。提交前运行 `git config user.name` 和 `git config user.email` 再确认一次。

### 0.3 开始修改前检查

```bash
git status --short
git branch --show-current
git remote -v
git log -1 --oneline
```

公开开发应位于 `main` 分支，远程应指向 `goldcook/goldcook.github.io`。如果工作区已有未提交改动，必须先理解改动来源，不能直接重置、覆盖或清理。

如果使用多个 Agent，只允许一个集成负责人修改主工作区；其他写代码的 Agent 必须使用独立 Git worktree。审计 Agent 应保持只读。

### 0.4 启动项目

项目没有安装依赖、构建步骤或后端服务。使用任意静态 HTTP 服务器即可：

```bash
python3 -m http.server 4173 --bind 127.0.0.1
```

然后分别检查：

- 所有者模式：<http://127.0.0.1:4173/>
- 公开访客模式：<http://goldcook.localhost:4173/>

所有者模式只在当前工作区存在 `private/timeline.local.js` 时显示可解锁的私人档案。公开访客模式必须和正式网站保持相同的隐私边界。

### 0.5 外部配置

公开 GitHub 仓库已经配置 Actions Secret `QQ_PLAYLIST_ID`，用于每周同步公开 QQ 音乐歌单。Secret 的值不在代码中，也不需要从浏览器登录状态恢复。

接手 Agent 不应读取、打印、提交或重新推断这个 Secret。除非用户明确更换歌单，否则保持现有 Secret 和工作流不变即可。

GitHub Pages 从公开仓库 `main` 分支根目录发布。项目没有自定义域名，正式地址始终是 <https://goldcook.github.io/>。

## 1. 一期定位

这个网站不是技术简历，也不是项目作品集，而是一份持续更新的个人主页。

一期采用温暖的 16-bit 像素 RPG 风格，把主页组织成一个可以探索的小世界：访客从开场进入世界地图，再选择不同地点了解 Goldcook 当下的工作、学习、生活和思考。网站保留成长记录与未来内容扩展能力，但不会为了填满页面而公开不成熟或过于私人的内容。

一期的核心表达是：

- 刚从学生阶段走进职场，在互联网从事算法相关工作。
- 保持学习，关注 AI 与强化学习，也学习怎样工作和生活。
- 工作不是全部，生活里还有音乐、小说短剧、联机游戏和看云。
- 正在理解成年人的身份、关系、责任与更长远的生活方式。
- 网站会继续生长，但不承诺固定更新频率。

## 2. 当前信息架构

### 开场

开场负责最短的自我介绍、人生进度氛围和玩家状态。它只保留一个主要行动：进入世界地图。

首页已经预留 `socialLinks` 数据入口。当前数组为空，因此社交媒体链接不会显示。以后确认需要公开的平台后，可以直接补充数据，不需要重新设计首页结构。

### 世界地图

世界地图是主页的主目录，当前包含六个并列区域：

| 区域 | 作用 |
| --- | --- |
| 工作城镇 | 当前工作状态、职场 Landing 与算法工作概览 |
| 学习营地 | 学会工作、学习前沿、学会生活 |
| 生活村落 | 音乐、小说短剧、联机游戏、看云 |
| 思考高塔 | 工作边界、新责任和想要怎样生活 |
| 杂谈书屋 | 未来收拢短文、小想法或外部原文链接 |
| 来信驿站 | 通过 Gmail 联系 |

地图区域与道路由 `content.js` 中的 `mapAreas` 自动生成。新增区域时仍需为新场景补充 HTML 和 CSS，但不需要手工绘制 SVG 道路或修改区域计数。

### Background

`BACKGROUND / 背景故事` 是地图边框上的特殊入口，不与六个普通地图区域并列。

公开页面只展示四个阶段的简短概览：儿时、中学、大学、进入职场。时间树节点、章节数量和树枝连线由 `lifeStages` 数据生成，可以继续增加阶段。

详细经历属于私人档案，不进入公开构建。本机使用 `localhost` 或 `127.0.0.1` 预览时，页面会尝试加载 `private/timeline.local.js`；正式域名不会加载该文件。

### 杂谈书屋

一期只保留一个有设计感的空状态：

- 标题：偶尔写点东西
- 空状态：这里暂时还没有

这里不是需要定期维护的独立博客。以后可以采用三种方式更新：

1. 在主页里放标题与短摘要。
2. 搬运一段愿意公开的完整文字，并链接到独立站内页面。
3. 只保留标题、摘要和其他社交平台的原文链接。

`notes` 条目支持无链接卡片，也支持站内相对链接和 HTTP(S) 外链。外链会在新标签页打开。

## 3. 视觉与交互原则

- 保持温暖、轻松的像素游戏世界，而不是后台面板或技术简历。
- 主要颜色是天空蓝、草地绿、奶油色、夕阳橙和深海军蓝。
- 像素感来自硬边框、块状阴影、地图语言和阶梯动画，不以牺牲可读性为代价。
- 桌面端的主要场景尽量严格控制在一屏内，不要求上下滚动。
- 手机端允许自然纵向滚动，优先保证字号、点击区域和内容完整性。
- 页面导航遵循“开场 → 地图 → 场景 → 返回地图”的游戏循环。
- 悬浮音乐播放器在所有场景都可以使用，手机端默认折叠成右下角按钮。
- 减少无必要的解释文案。界面本身能够表达含义时，不重复说明。

完整设计约束见根目录的 `DESIGN.md`。

## 4. 代码结构

| 文件 | 职责 |
| --- | --- |
| `index.html` | 所有场景的语义结构、固定文案、SEO 和资源版本号 |
| `content.js` | 地图、成长阶段、学习、生活、思考、杂谈、社交入口和音乐数据 |
| `script.js` | 场景路由、地图与时间树生成、私人档案加载、音乐播放器和动态内容渲染 |
| `styles.css` | 像素视觉、地图地形、桌面单屏布局和手机响应式布局 |
| `scripts/update-qq-playlist.mjs` | 从公开 QQ 音乐歌单读取前三首并更新 `content.js` |
| `.github/workflows/sync-qq-music.yml` | 每周运行歌单同步，也支持手动触发 |
| `private/` | 私人时间线与整理材料，只存在于本机和私有备份 |

网站没有框架和构建步骤，使用原生 HTML、CSS 和 JavaScript，可以直接由 GitHub Pages 托管。

### 运行时约定

- 每个可导航场景使用 `data-screen="screen-id"`。
- 所有入口使用 `data-screen-target="screen-id"`，由 `script.js` 统一处理哈希、浏览器历史、焦点和返回地图逻辑。
- 公开场景会加入 `validScreens`；`private-archive` 只有在本机成功加载私人脚本后才会启用。
- 地图节点写入 `[data-map-nodes]`，地图 SVG 道路写入 `[data-map-routes]`。
- 时间树节点写入 `[data-tree-nodes]`，树枝写入 `[data-tree-routes]`。
- 可重复内容优先放在 `content.js`，不要在 HTML 中复制一份相同数据。
- 页面没有服务端能力。任何留言、表单、数据库或定时内容都需要可信的外部服务或 GitHub Actions。

### 缓存版本

`index.html` 通过 `?v=YYYYMMDD-NN` 给 `favicon.svg`、`styles.css`、`content.js` 和 `script.js` 添加缓存版本。修改样式或脚本并正式发布时，应同时递增四处版本号，避免 GitHub Pages 或浏览器继续使用旧资源。

`content.js` 中的 `siteEdition` 是页面向访客展示的人工版本标记。自动更新 QQ 音乐时不要修改它；只有用户确认一次新的主页版本时才更新。

## 5. 可扩展数据

### 新增地图区域

在 `content.js` 的 `mapAreas` 添加：

```js
{ name: "区域名称", icon: "图标", x: 50, y: 50, target: "screen-id" }
```

然后在 `index.html` 增加对应的 `data-screen="screen-id"` 场景，并在 `styles.css` 中设计场景和地图地形。道路和区域计数会自动生成。

继续增加区域前必须重新检查手机点击热区。当前六个地点加旅程起点在 `390×844` 下没有重叠，但地图下半区已经比较密集。

### 新增成长阶段

在 `lifeStages` 添加包含 `code`、`title`、`summary`、`tags`、`x`、`y`、`accent` 和可选 `privateId` 的对象。树干、分支、节点序号和章节计数会自动更新。

公开 `summary` 应保持概览性质，不包含城市、学校、公司、具体住址或能够组合定位个人的信息。

### 新增杂谈

在 `notes` 添加：

```js
{
  tag: "随想",
  title: "标题",
  excerpt: "两三句公开摘要",
  url: "https://example.com/or/local-page"
}
```

`url` 可以省略。省略后会显示为站内普通卡片；填写 HTTP(S) 地址时会显示为外链。当前实现会拒绝 `javascript:`、`data:` 等不安全协议。

如果要放完整长文，建议新建独立页面或内容目录，再从杂谈书屋链接过去，避免把主场景变成长滚动列表。

### 新增社交入口

在 `socialLinks` 添加：

```js
{ label: "平台名称", url: "https://example.com/profile" }
```

只公开已经确认愿意长期展示的平台。数组为空时，首页不会留下空白区域或占位按钮。

## 6. 音乐方案

悬浮播放器有两部分：

- 三段通过 Web Audio API 实时生成的原创像素 BGM。
- 三个“最近在听”的 QQ 音乐官方歌曲页面入口。

原创 BGM 只使用宽泛的流行、轻松和游戏音乐气质，不复制或改编可识别的受版权保护旋律。主页不抓取、不代理、不托管 QQ 音乐音频。

QQ 音乐同步流程：

1. GitHub Actions 每周一运行同步任务。
2. 工作流从仓库 Secret `QQ_PLAYLIST_ID` 读取歌单编号。
3. 脚本读取公开歌单前三首，更新 `content.js` 标记区间。
4. 只有歌曲发生变化时，GitHub Actions 才会自动提交。

歌单编号、QQ 账号信息和浏览器登录状态都不应写入公开代码。

## 7. 隐私边界

公开站点可以展示：

- Goldcook 这个名字。
- Gmail 联系邮箱。
- 互联网算法工作的概括。
- AI、强化学习、工作、学习、生活和兴趣方向。
- 经过人工确认的成长阶段概览与杂谈。

公开站点不展示：

- 公司、团队、学校和具体城市。
- 旧居、精确成长地点或能够组合定位的信息。
- 私人日记、照片、社交动态和详细个人经历。
- 未经确认的家人、朋友、伴侣相关信息。
- QQ 歌单编号和其他账户标识。

私人内容只保存在私有备份仓库的 `private/` 中。公开仓库通过 `.gitignore` 忽略整个 `private/` 目录。发布前必须运行 `git ls-files 'private/**'`，确认输出为空。

不要只依赖 `.gitignore`。每次发布前还应搜索本次讨论中可能出现的真实地点、学校、公司、人物姓名、账号链接和私人摘录。公开概览可以描述阶段和主题，但不能把私人档案改写后直接批量发布。

## 8. 本地预览与公开模式

启动本地服务器：

```bash
python3 -m http.server 4173 --bind 127.0.0.1
```

两个预览地址用途不同：

- `http://127.0.0.1:4173/`：本机所有者模式，可以加载 `private/timeline.local.js`。
- `http://goldcook.localhost:4173/`：公开访客模式，不加载私人档案，适合发布前检查。

不要直接双击 `index.html` 预览，因为文件协议不能准确模拟路由、脚本和私人档案边界。

## 9. 测试基线

每次正式发布前至少执行：

```bash
node --check content.js
node --check script.js
node --check scripts/update-qq-playlist.mjs
git diff --check
git ls-files 'private/**'
```

浏览器需要检查：

- 桌面 `1466×871`：首页、地图和各主要场景保持单屏，无页面级溢出。
- 手机 `390×844`：无横向滚动，地图节点可点击且点击热区不重叠。
- `goldcook.localhost`：不加载私人脚本，不出现私人档案正文。
- 悬浮播放器：展开、播放、暂停、切歌和音量控制正常。
- 地图循环：每个地点都能进入并返回地图，浏览器前进后退正常。

## 10. 发布与备份

公开仓库的 `main` 分支由 GitHub Pages 直接发布到 <https://goldcook.github.io/>。

正式发布流程：

1. 完成本地语法、隐私和响应式检查。
2. 使用 `goldcook <goldcook4@gmail.com>` 作为提交身份。
3. 提交并推送公开仓库 `main`。
4. 等待 GitHub Pages 更新，并确认线上 HTML 使用最新资源版本号。
5. 将相同站点文件同步到私有完整备份仓库。
6. 不要用公开 README 或 `.gitignore` 覆盖私有仓库的安全警告与私人文件跟踪规则。

私有备份仓库必须始终保持 Private，不能启用 GitHub Pages，也不能将其历史合并回公开仓库。

### 推荐的双仓库同步方法

以公开工作区作为网站代码的集成源。公开版本验证完成后，将这些文件同步到私有仓库：

- `.github/`
- `scripts/`
- `docs/`
- `.nojekyll`
- `404.html`
- `DESIGN.md`
- `content.js`
- `favicon.svg`
- `index.html`
- `script.js`
- `styles.css`

以下内容不能从公开仓库覆盖到私有仓库：

- 私有仓库的 `README.md`，因为它包含醒目的私有备份警告。
- 私有仓库的 `.gitignore`，因为它需要跟踪 `private/`。
- 私有仓库的 `private/` 目录。
- 私有仓库的 `.git/` 历史和远程配置。

同步后对公共文件逐一执行 `cmp` 或等价比较，再分别在两个仓库提交和推送。公开仓库与私有仓库使用不同提交，不做 cherry-pick、merge 或 force-push 来保持同步。

### 发布完成的判断标准

只看到 `git push` 成功不代表 Pages 已经更新。发布后应请求正式首页并确认 HTML 中引用了最新的 `?v=` 资源版本，然后在正式域名做一次桌面和手机检查。

## 11. 下一阶段候选方向

下一阶段不需要一次全部实现，可以按真实内容逐步生长：

- 为杂谈书屋添加第一条内容，决定是站内全文还是外部链接。
- 确认愿意公开的社交媒体，再启用首页社交入口。
- 为长文章设计独立详情页和返回杂谈书屋的导航。
- 继续补充 Background 阶段，但先明确公开概览与私人记录的边界。
- 如果地图区域继续增加，考虑二级地图、分页地图或特殊入口，避免主地图过密。
- 根据真实手机使用感受继续调整字号、地图高度和悬浮播放器位置。
- 如果未来需要匿名留言，再选择可信的表单服务；不要在纯静态页面中直接暴露密钥。

### 当前明确没有实现的功能

- 杂谈书屋目前没有真实文章或外链。
- 首页目前没有启用任何社交媒体入口。
- 来信驿站只有 `mailto:`，没有匿名留言后端。
- QQ 音乐只提供官方歌曲页面入口，没有在站内代理或嵌入完整音频。
- 私人档案没有远程登录或远程解锁能力，只支持本机静态文件。
- 网站没有 CMS、数据库、搜索、统计分析或自动抓取社交媒体内容。

## 12. 下一次接续开发

开始下一次修改前，先确认三件事：

1. 这次内容是公开信息、私人记录，还是仅供本机预览。
2. 新内容应该进入现有区域，还是值得成为新的地图地点。
3. 发布前是否已经在公开访客模式、桌面和手机三个环境检查。

一期已经完成了稳定的地图导航、内容边界和扩展骨架。下一阶段应优先增加真实想分享的内容，而不是为了丰富页面继续堆叠装饰或说明。

### 给接手 Agent 的完成清单

一次修改只有满足以下条件才算完成：

1. 已说明改动属于公开内容还是私人内容。
2. 已在公开访客模式验证没有私人脚本或私人正文。
3. 已运行语法检查、`git diff --check` 和 `git ls-files 'private/**'`。
4. 已检查桌面 `1466×871` 与手机 `390×844`。
5. 地图新增节点时已检查所有点击热区互不重叠。
6. 修改 CSS 或 JavaScript 时已递增 `index.html` 的资源缓存版本。
7. 公开提交使用 Goldcook Gmail 身份，并已确认远程仓库正确。
8. 正式发布必须得到用户明确确认，预览确认不能自动视为发布授权。
9. 公开发布完成后，已同步并推送私有完整备份。
10. 最终回复应提供正式网址、提交编号、验证结果和仍未实现的内容。
